Too many global variables. I have to clean up.

mapArray, offset, playerEntities, buildingEntites etc.

remove global variables from updateEntities.
change name. Those are map related

why is the graphics all messed up? 